package ${Mapper.classPackageName};

import ${Entity.classPackageName};
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.baomidou.mybatisplus.core.metadata.IPage;
import java.util.List;
import java.util.Map;

/**
 * ${Mapper.tableComment}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${Mapper.className}Mapper extends BaseMapper<${Entity.className}Entity> {
	/**
	 * 查询列表信息
	 * @param params
	 * @return
	 */
	List<${Entity.className}Entity> queryPage(IPage<${Entity.className}Entity> page, Map<String, Object> params);
}
